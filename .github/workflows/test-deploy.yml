name: LEM deployment

env:
  AWS_REGION: "us-east-1"
  TF_VAR_name: "edgemanager-2.25.0-github-workflow"
  TF_VAR_oem_name: "edgemanager"
  TF_VAR_app_version: "2.25.0"

on:
  workflow_dispatch:
    inputs:
      RUN:
        description: "What action to perform?"
        required: true
        default: "deploy"
        type: choice
        options:
          - plan
          - deploy
          - destroy
      CLOUD:
        description: "Which cloud provider?"
        required: true
        default: "aws"
        type: choice
        options:
          - aws
          - azure

jobs:
  lem-tf-plan-aws:
    name: tf-plan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: aws/examples/edgemanager-aws
    env:
      STATE: ${{ secrets.TF_STATE_CONTENT_AWS }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VAR_vpc_id: ${{ secrets.VPC_ID }}
      TF_VAR_subnet_id: ${{ secrets.SUBNET_ID }}
      TF_VAR_key_name: ${{ secrets.KEY_NAME }}
      TF_VAR_ami_owner: ${{ secrets.AMI_OWNER }}
      TF_VAR_ssh_pub_key: ${{ secrets.SSH_PUB_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: set state
        run: |
          printf "%s" "$STATE" > state.tf

      - name: tf-init
        run: terraform init

      - name: tf-plan
        id: plan
        run: terraform plan

  lem-tf-deploy-aws:
    name: tf-deploy
    needs: lem-tf-plan-aws
    if: ${{ github.event.inputs.RUN == 'deploy' && github.event.inputs.CLOUD == 'aws' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: aws/examples/edgemanager-aws
    env:
      STATE: ${{ secrets.TF_STATE_CONTENT_AWS }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VAR_vpc_id: ${{ secrets.VPC_ID }}
      TF_VAR_subnet_id: ${{ secrets.SUBNET_ID }}
      TF_VAR_key_name: ${{ secrets.KEY_NAME }}
      TF_VAR_ami_owner: ${{ secrets.AMI_OWNER }}
      TF_VAR_ssh_pub_key: ${{ secrets.SSH_PUB_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: set state
        run: |
          printf "%s" "$STATE" > state.tf

      - name: tf-init
        run: terraform init

      - name: tf-apply
        id: apply
        run: terraform apply -auto-approve

  lem-tf-destroy-aws:
    name: tf-destroy
    needs: lem-tf-plan-aws
    if: ${{ github.event.inputs.RUN == 'destroy' && github.event.inputs.CLOUD == 'aws' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: aws/examples/edgemanager-aws
    env:
      STATE: ${{ secrets.TF_STATE_CONTENT_AWS }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VAR_vpc_id: ${{ secrets.VPC_ID }}
      TF_VAR_subnet_id: ${{ secrets.SUBNET_ID }}
      TF_VAR_key_name: ${{ secrets.KEY_NAME }}
      TF_VAR_ami_owner: ${{ secrets.AMI_OWNER }}
      TF_VAR_ssh_pub_key: ${{ secrets.SSH_PUB_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: set state
        run: |
          printf "%s" "$STATE" > state.tf

      - name: tf-init
        run: terraform init

      - name: tf-destroy
        id: destroy
        run: terraform destroy -auto-approve

  lem-tf-plan-azure:
    name: tf-plan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: azure/examples/edgemanager-azure
    env:
      STATE: ${{ secrets.TF_STATE_CONTENT_AZURE }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: set state
        run: |
          printf "%s" "$STATE" > state.tf

      - name: tf-init
        run: terraform init

      - name: tf-plan
        id: plan
        run: terraform plan